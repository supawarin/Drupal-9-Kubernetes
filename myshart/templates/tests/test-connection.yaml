apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myshart.fullname" . }}-test-connection"
  labels:
    {{- include "myshart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myshart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
