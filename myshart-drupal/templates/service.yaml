apiVersion: v1
kind: Service
metadata:
  name: {{ include "myshart.fullname" . }}
  labels:
    {{- include "myshart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myshart.selectorLabels" . | nindent 4 }}
